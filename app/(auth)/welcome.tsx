import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Onboarding = () => {
  return (
    <SafeAreaView>
      <Text>Welcome Abroad Broad your time to shine is near</Text>
    </SafeAreaView>
  );
};

export default Onboarding;
